# Example environment variables for local development
# Copy to .env.local (Next.js picks up NEXT_PUBLIC_ prefixed vars in the browser)

# The public site URL Shopify should return users to after checkout
NEXT_PUBLIC_SITE_URL=https://melanvee.com

# Add other env vars used by the app below
